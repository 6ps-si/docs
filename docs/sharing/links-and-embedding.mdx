---
title: "Share links and embedding"
description: "Send a link, show a QR code, or put a project inside your own site."
---

<Note>
  Outline only. Verify every statement against the source files listed at the bottom of
  this page before publishing. Do not state a capability, limit, or price that those
  files do not confirm.
</Note>

{/* SCREENSHOT NEEDED: Export dialog with the link, QR code, and embed snippet visible
    Save to images/<section>/ then replace this comment with:
    <Frame><img src="/images/<section>/<name>.png" alt="..." /></Frame> */}

## Sharing a link

Copying the viewer link, and what a visitor sees on that page.

## QR codes

Generating a QR code and downloading it as an image. Useful on printed material and on site.

## Embedding in a site

Copying the iframe snippet and pasting it into your own page.

## Embedding in WordPress

The WordPress snippet, offered from the viewer page.

## Restricting where it can be embedded

Domain locking limits embedding to approved sites. Link back to visibility.

## Downloading a package

The downloadable package and code embed are available on the Pro plan. State the gate
without prices.

---

{/*
Sources of truth for this page:
  - ../Studio/components/Editor/ExportDialog.tsx
  - ../Studio/app/viewer/page.tsx
  - ../Studio/app/view/page.tsx
  - ../Studio/hooks/useQrCode.ts
*/}
