---
title: "Hotspots and buttons"
description: "Add clickable points that show text, images, or links inside a scene."
---

<Note>
  Outline only. Verify every statement against the source files listed at the bottom of
  this page before publishing. Do not state a capability, limit, or price that those
  files do not confirm.
</Note>

{/* SCREENSHOT NEEDED: A scene with several hotspots placed, one open and showing its content
    Save to images/<section>/ then replace this comment with:
    <Frame><img src="/images/<section>/<name>.png" alt="..." /></Frame> */}

## Two kinds of buttons

Buttons placed in the 3D scene, which sit at a point on the model, and buttons fixed to the
screen, which stay put as the camera moves. Explain when each is right.

## Placing a button

Drag to place it, then adjust its position.

## Writing the content

The rich text editor: formatting, color, links, alignment.

## Appearance

Icon, size, and color options. Verify the exact set in the settings panels.

## Editing and removing

Selecting an existing button and deleting it.

---

{/*
Sources of truth for this page:
  - ../Studio/components/Editor/ButtonsPanel.tsx
  - ../Studio/components/Editor/ButtonsCanvasPanel.tsx
  - ../Studio/components/Editor/RichTextEditor.tsx
  - ../Studio/hooks/useButtonDrag.ts
*/}
