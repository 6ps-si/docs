---
title: "Camera positions"
description: "Save named views so visitors can move through a model in a set order."
---

<Note>
  Outline only. Verify every statement against the source files listed at the bottom of
  this page before publishing. Do not state a capability, limit, or price that those
  files do not confirm.
</Note>

{/* SCREENSHOT NEEDED: Positions panel beside a position's settings, showing the constraint sliders
    Save to images/<section>/ then replace this comment with:
    <Frame><img src="/images/<section>/<name>.png" alt="..." /></Frame> */}

## Saving a view

Frame the model, then capture the current view as a position.

## Naming and describing

Name and description, and where viewers see them.

## Movement between positions

Animation duration and what it controls.

## Framing

Field of view, and the zoom and rotation limits on the X and Y axes.

## Look-at targets

Picking a point for the camera to face.

## Ordering

Reordering positions changes the sequence visitors follow.

---

{/*
Sources of truth for this page:
  - ../Studio/components/Editor/PositionsPanel.tsx
  - ../Studio/components/Editor/PositionSettingsPanel.tsx
  - ../Studio/components/Editor/ConstraintSlider.tsx
*/}
